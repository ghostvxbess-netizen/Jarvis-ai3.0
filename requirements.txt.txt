# Джарвис v2 — зависимости
streamlit>=1.35.0
groq>=1.3.0

# PostgreSQL (опционально — только если задан DATABASE_URL)
# Без него работает SQLite — раскомментируй при необходимости:
# psycopg2-binary>=2.9.9
